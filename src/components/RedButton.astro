---
const Link = Astro.props.Link;
const Text = Astro.props.Text;

---

<a href={Link}>{Text}</a>

  <style>
    a{
        display: flex;
width: 300px;
height: 60px;
padding: 14px 78px;
justify-content: center;
align-items: center;
gap: 10px;
flex-shrink: 0;
border-radius: var(--Corner-radius, 30px);
background: var(--Rd-Primr, #D71330);
    }
  </style>